# ============================================
# Production Environment Configuration
# ============================================
#
# This file configures Docker Compose variables for production.
# Copy this file to `.env` and update with your actual values.
#
# IMPORTANT: This .env file is for Docker Compose only.
# Backend secrets are stored in ./secrets/ directory.
# ============================================

# Domain Configuration
# Your production domain (e.g., tuberia.app) or server IP
DOMAIN_NAME=localhost

# SSL/TLS Email for Let's Encrypt certificate notifications
SSL_EMAIL=admin@example.com

# ============================================
# Production Setup Instructions:
# ============================================
#
# 1. Copy this file:
#    cp .env.example .env
#
# 2. Edit .env with your domain:
#    DOMAIN_NAME=your-domain.com
#    SSL_EMAIL=your-email@example.com
#
# 3. Configure DNS:
#    Point your domain to your server's IP address
#
# 4. Configure backend secrets:
#    See ./secrets/README.md for instructions
#
# 5. Deploy:
#    docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d
# ============================================