# Este archivo se deja intencionadamente casi vacío.
# Usar solo para overrides locales y temporales durante el desarrollo.
# La configuración principal está en docker-compose.yml y docker-compose.dev.yml.
