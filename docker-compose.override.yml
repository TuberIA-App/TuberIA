## Archivo override vacío. Usar solo para cambios locales puntuales.
    restart: unless-stopped

    # Explicitly run dev command (nodemon)
    command: npm run dev

    # Health check for development
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:5000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  mongo:
    # Expose MongoDB port for local development/debugging tools
    ports:
      - "27017:27017"

    # Development credentials (NOT for production!)
    environment:
      - MONGO_INITDB_ROOT_USERNAME=mongo
      - MONGO_INITDB_ROOT_PASSWORD=mongo
      - MONGO_INITDB_DATABASE=tuberia_db

    # Restart policy for development
    restart: unless-stopped